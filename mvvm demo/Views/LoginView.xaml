<Window x:Class="YourApp.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:mvvm_demo"
        Width="300"
        Height="300"
             mc:Ignorable="d"
        DataContext="{StaticResource LoginVM}">
    <Grid VerticalAlignment="Center"
          HorizontalAlignment="Center"
          Width="200">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username:" Grid.Row="0" Margin="10"/>
        <TextBox Text="{Binding Username,UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Margin="10" />

        <TextBlock Text="Password:" Grid.Row="2" Margin="10"/>
        <PasswordBox Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Margin="10" />

        <Button Content="Login" Grid.Row="4" Margin="10" Command="{Binding LoginCommand}"/>
    </Grid>
</Window>